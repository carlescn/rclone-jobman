# Descriptive name for the sync job
jobName="This job is an example"

# For testing purposes, set to TRUE and rclone will NOT write anything to the remote server.
dryrun=FALSE

# Paths for source (READ) and destination (WRITE)
sourcePath=/path/to/source
destinationPath=rclone_remote_name:/path/to/destination/
